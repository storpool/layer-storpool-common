name: layer-storpool-common
summary: Install the storpool-common package and some config files
maintainer: StorPool <support@storpool.com>
description: |
  This layer installs a Debian package and prepares some basic StorPool
  configuration files.
